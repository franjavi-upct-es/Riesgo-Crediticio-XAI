# infra/k8s/secret.yml
#
# IMPORTANT: Replace the base64-encoded values before applying.
#   echo -n "your-api-key" | base64
#
# In production, use an external secrets manager (AWS Secrets Manager,
# HashiCorp Vault, or Sealed Secrets) instead of plain K8s secrets.
apiVersion: v1
kind: Secret
metadata:
  name: credit-risk-secrets
  namespace: credit-risk
  labels:
    app.kubernetes.io/name: credit-risk-api
    app.kubernetes.io/part-of: credit-risk-xai
type: Opaque
data:
  # echo -n "change-me-in-production" | base64
  API_API_KEY: Y2hhbmdlLW1lLWluLXByb2R1Y3Rpb24=
